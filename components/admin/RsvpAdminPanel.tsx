"use client";

import { RsvpGuestListAdmin } from "@/components/admin/RsvpGuestListAdmin";

/** Single admin surface: guest list, status filters, and per-guest RSVP responses. */
export function RsvpAdminPanel() {
  return <RsvpGuestListAdmin />;
}
